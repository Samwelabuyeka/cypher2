/**
* Validation code for field fromAddress on cypherTransaction
* @param {import("gadget-server").CypherTransactionFromAddressFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.fromAddress` to get the fields's data
  // add to the `errors` with `errors.add('fromAddress', "did not pass the validation")`
};