/**
* Validation code for field cryptoAddress on paymentMethod
* @param {import("gadget-server").PaymentMethodCryptoAddressFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.cryptoAddress` to get the fields's data
  // add to the `errors` with `errors.add('cryptoAddress', "did not pass the validation")`
};