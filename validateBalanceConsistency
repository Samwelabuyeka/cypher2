/**
* Validation code for field balance on wallet
* @param {import("gadget-server").WalletBalanceFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.balance` to get the fields's data
  // add to the `errors` with `errors.add('balance', "did not pass the validation")`
};